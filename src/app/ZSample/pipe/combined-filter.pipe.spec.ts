import { CombinedFilterPipe } from './combined-filter.pipe';

describe('CombinedFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CombinedFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
