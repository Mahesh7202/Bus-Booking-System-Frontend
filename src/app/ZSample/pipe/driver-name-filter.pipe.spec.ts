import { DriverNameFilterPipe } from './driver-name-filter.pipe';

describe('DriverNameFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DriverNameFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
