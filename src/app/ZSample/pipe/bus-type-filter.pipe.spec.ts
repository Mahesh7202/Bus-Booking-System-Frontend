import { BusTypeFilterPipe } from './bus-type-filter.pipe';

describe('BusTypeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new BusTypeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
