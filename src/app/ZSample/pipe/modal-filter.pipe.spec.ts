import { ModalFilterPipe } from './modal-filter.pipe';

describe('ModalFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ModalFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
